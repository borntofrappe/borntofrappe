---
layout: null
---

{% block style %}
  {% include "css/blog.css" %}
{% endblock %}

{% extends "layouts/base.njk" %}
{% set hasContent = true %}

{% block content %}

<header>
<h1>Jotting things down</h1>
  <p>I write so I can truly learn something. I write because I tend to forget.</p>
  <p>If it saves a Google search or two, I figure this a complete success.</p>
</header>

<main id="content">
  {% for post in collections.blog %}
  <article>
    <h2>
      <a href="{{post.url}}">
        {{ post.data.title }}
        {% if loop.index0 === 0 %}
          {{ icons.sparkles | safe }}
        {% endif %}
      </a>
    </h2>

    {% set datetime = post.data.datetime or post.date %}
    {% set date = datetime | formatdate %}
    <time datetime="{{datetime}}">
      {{ date }}
    </time>


    {% if post.data.brif %}
      <p>
      {{ post.data.brief }}
      </p>
    {% endif %}
  </article>
  {% endfor %}
</main>

{% endblock %}