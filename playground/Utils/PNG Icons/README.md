# PNG Icons

Create `.png` icons for the website.

## svg - canvas - png

In the demo, I recreate the syntax behind the letter `b` icon with the canvas API. I then use an anchor link element to download the associated data and obtain the static asset.
